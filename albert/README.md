# obw's albert modules

## MultiMC

Indexes your MultiMC instances and lets you launch them.
Requires config file at `$HOME/.config/albert_modules.json` with the following:

```json
{
    "multimc_home": "/path/to/your/MultiMC"
}
```
